'use strict';

var $ = require('jQuery');

var nav = require( 'nav' );
jQuery( nav );

var slider = require( 'slider' );
jQuery( slider );

var video_modal = require( 'video-modal' );
jQuery( video_modal );
